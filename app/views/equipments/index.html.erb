<div class="cards-container">
  <div class="row">
    <% @equipments.each do |equipment| %>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <%= link_to equipment_path(equipment.id), class: "card" do %>
          <h2><%= equipment.title %></h2>
          <p><%= equipment.description %></p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
